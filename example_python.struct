<CODE: PYTHON>
    {{IMPORT_NUMPY}}
    {{IMPORT_MATPLOTLIB}}
    
    # Create some sample data
    x = np.linspace(0, 10, 100)
    y = np.sin(x)
    
    # Plot the data
    plt.figure(figsize=(10, 6))
    plt.plot(x, y, 'b-', linewidth=2)
    plt.title('Sine Wave')
    plt.xlabel('X values')
    plt.ylabel('Y values')
    plt.grid(True)
    plt.show()
    
    {{IF_NAME_MAIN}}
        {{PRINT_DEBUG}}
        {{TRY_EXCEPT}}
</CODE>

<COMMANDS>
    # Save the file
    ENTER 2
</COMMANDS>